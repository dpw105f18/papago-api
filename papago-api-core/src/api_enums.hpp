#pragma once
//TODO: move diz!
using Format = vk::Format;
using SwapChainPresentMode = vk::PresentModeKHR;

enum TypeEnums {
	S32FLOAT
};

enum DepthTest {
	LESS
};

enum ImageType {
	IMAGE_DEPTH_BUFFER
};